function extractFile (str) {
    let directrion = str.split('\\');
    let fileNameExtension = directrion.endsWith();

    console.log(directrion)
}
extractFile('C:\\Internal\\training-internal\\Template.pptx');