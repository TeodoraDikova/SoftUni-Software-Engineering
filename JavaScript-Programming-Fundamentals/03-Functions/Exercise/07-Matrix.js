function matrix(num){
    
    for (let rowCount = 0; rowCount < num; rowCount++) {
        let arr = [];

        for (i = 0; i < num; i++) {
            arr.push(num);
        }

        console.log(arr.join(" "));
    }
}